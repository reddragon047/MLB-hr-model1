name: Daily HR Board

on:
  schedule:
    - cron: "0 22 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas numpy pybaseball scikit-learn xgboost mlb-statsapi pyarrow

      - name: Run HR model
        run: |
          echo "HR model placeholder — we will add the real model next step."
